var expect = require(`chai`).expect;

describe(`checkForShip`, function (){
    var checkForShip = require(`../game_logic/ship_methods`).checkForShips;

    it('should correctly report no ship at the given players coordinate ', function () {
        
    });
})